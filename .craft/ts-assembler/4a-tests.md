## Carry
slug: ts-assembler | branch: feature/ts-assembler | root: /mnt/workspace/code/isoroll-module
test-cmd: `npm run verify:fast` | e2e-cmd: golden diff vs Python output (Loop 5 scripts; Python outputs at /mnt/workspace/code/isoroll-content/output/assembled/l-room_{NW,NE,SW,SE}.png)
criticality: normal | verdict: standard
criteria:
  C1 — TS port of the deterministic per-cell assembler (`scene_assemble.py` twin) in `src/assemble/`: input = layout grid (parsed model) + kit meta (kit.json shape: px_per_unit, pieces{origin,size}) + kit piece textures; output = composed scene image (PIXI RenderTexture or offscreen canvas) for a given view (SW/SE/NE/NW)
  C2 — semantics EXACTLY match Python: view rotation = real coordinate rotation of the grid (rotate never mirror), painter order = (h>0, u0+v0), piece selection = floor | wall | {door,window}_{axis} with horizontal-run-wins-ties axis rule, placement = proj(u0,v0,0) − piece origin, proj(u,v,z) = ((u−v)s, (u+v)s/2 − zs)
  C3 — layout parsing: minimal TS twin of `layout_parse.py` for the DSL subset (#/./D/W/space; name/wall_h directives) with the same validation (door/window must sit in a wall run); stairs cells tolerated but skipped (assembly v1 parity)
  C4 — golden test: assembled l-room (all 4 views) pixel-diffs ≤1% vs the Python PNGs (allow AA/codec tolerance), automated in the e2e or unit harness
tasks: T1 assets — copy full kit(json+6png)+4 goldens — test/unit/assets/* — low | T2 types — src/assemble/types.ts — medium | T3 layout-parse — src/assemble/layout-parse.ts — medium | T4 massing — src/assemble/massing.ts — medium | T5 assemble core — src/assemble/assemble.ts — high | T6 facade+CONTEXT — src/assemble/index.ts,CONTEXT.md — low | T7 parse/massing unit tests — test/unit/assemble-parse.test.ts — medium | T8 golden test+compositor — test/unit/assemble-golden.test.ts,test/unit/helpers/composite.ts — high
context: /mnt/workspace/code/isoroll-module/CONTEXT.md, /mnt/workspace/code/isoroll-module/src/CONTEXT.md, /mnt/workspace/core/skills/foundry.md, /mnt/workspace/core/skills/iso-visual.md, /mnt/workspace/code/isoroll-content/SCENE-CREATION.md (§ contract), Python reference: /mnt/workspace/code/isoroll-content/src/pipeline/{scene_assemble.py,layout_parse.py,layout_massing.py,scene_guide_render.py}
criticality-note: this becomes the painter's live re-render engine (P7) — keep it PURE (no Foundry document writes; rendering only), so painter can call it per stroke.

## Tests

Scaffolding: T1 assets copied verbatim to `test/unit/assets/` (kit.json + 6 piece PNGs + 4 goldens + `l-room.txt`, byte-diffed identical to the pipeline source). Interface-only stubs created for T2/T3/T4/T5/T6 (`src/assemble/{types,layout-parse,massing,assemble,index}.ts`) and the T8 test helper (`test/unit/helpers/composite.ts`): `types.ts` is real (pure types, no logic, per 3-arch.md); every other function is a signature-correct one-line `throw new Error("not implemented")` body — no logic — so the test files import cleanly and go red on real assertions in Loop 4b, not on module-resolution errors. This scaffolding is *not* implementation; Loop 4b fills every stub body.

Oracle values (rotateCw grid, validate errors, massing box list, painter order, pieceFor names, one hand-computed placement) were cross-checked by running the actual Python twins (`layout_parse.py`/`layout_massing.py`/`scene_assemble.py`) against the fixture grid and l-room.txt in this session — not hand-derived — to guarantee C2/C3 parity is encoded correctly before Loop 4b writes to green.

| test file | covers | asserts |
|-----------|--------|---------|
| `test/unit/assemble-parse.test.ts` | C2, C3 (seams: `layout-parse.ts`, `massing.ts`, `assemble.ts`) | `parseText`: directives (name/wall_h) + rectangular grid, no errors on valid fixture. `rotateCw`: exact grid after one CW turn (`zip(*grid[::-1])` twin). `validate` (via `parseText`): unknown-cell error `"(1,0) unknown cell 'X'"` with a stair char tolerated (no error); D-not-in-wall-run error `"(3,1) 'D' not inside a straight wall run"`. `massing`: full 19-box list for a 5×5 fixture (one merged 3-wide floor run per row = 3 floor boxes; 16 cell-level 1×1 wall boxes incl. one `door` opening axis `u` and one `window` opening axis `v`) — box shape and insertion order (floors then row-major walls) asserted exactly. `pieceFor`: name list over those 19 boxes = `floor×3, wall, door_u, wall×8, window_v, wall×5`. `planScene`: `placements` stable-sorted by `(h>0, u0+v0)` (19 entries, painter order asserted as a name list); `placements[4]` (the door_u box) asserted `{piece:"door_u", left:-8, top:-248}` — hand-computed from `proj(1,0,0)=(96,48)` minus kit door_u origin `[104,296]`. |
| `test/unit/assemble-golden.test.ts` (+ `test/unit/helpers/composite.ts`) | C1, C4 (seams: `assemble.ts` planScene, compositor helper, kit metadata) | Kit-metadata parity: every `kit.pieces[name].size` in `test/unit/assets/kit.json` equals the actual PNG `[width,height]` loaded via `loadTextures` (guards the pure-planner/compositor size contract). Per view (SW/SE/NE/NW, `describe.each`): `l-room.txt` parses with no errors; `planScene(layout, kit, view)` → `composite(plan, textures)` produces a PNG whose width/height match the golden exactly and whose `pixelmatch` diff ratio vs `test/unit/assets/l-room_{view}.png` is ≤1% (`threshold: 0.12` per-pixel AA tolerance, matching the project's existing `test/e2e/golden.mjs` convention). |
red-run: 12 failed as expected | wrong-failures: none

Verified: `rtk proxy npm run verify:fast` → lint clean (0 errors on new `src/assemble/*.ts` stubs); vitest: 8 pre-existing test files still green (40 tests, unchanged from Loop 2's ground check), 2 new test files fail with all 12 new tests red. Every new failure is `Error: not implemented` thrown from a stub production function (`parseText`, `massing`, `pieceFor`, `planScene`, `loadTextures`) at an assertion call site inside the test — none are TS/module-resolution or test-syntax errors. This is the correct red: the seams exist and are exercised, only the bodies are missing.

executor: loop-medium model=sonnet tier=medium
