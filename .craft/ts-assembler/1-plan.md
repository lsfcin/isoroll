## Carry
slug: ts-assembler | branch: feature/ts-assembler | root: /mnt/workspace/code/isoroll-module
test-cmd: `npm run verify:fast` | e2e-cmd: golden diff vs Python output (Loop 5 scripts; Python outputs at /mnt/workspace/code/isoroll-content/output/assembled/l-room_{NW,NE,SW,SE}.png)
criticality: normal | verdict: standard
criteria:
  C1 — TS port of the deterministic per-cell assembler (`scene_assemble.py` twin) in `src/assemble/`: input = layout grid (parsed model) + kit meta (kit.json shape: px_per_unit, pieces{origin,size}) + kit piece textures; output = composed scene image (PIXI RenderTexture or offscreen canvas) for a given view (SW/SE/NE/NW)
  C2 — semantics EXACTLY match Python: view rotation = real coordinate rotation of the grid (rotate never mirror), painter order = (h>0, u0+v0), piece selection = floor | wall | {door,window}_{axis} with horizontal-run-wins-ties axis rule, placement = proj(u0,v0,0) − piece origin, proj(u,v,z) = ((u−v)s, (u+v)s/2 − zs)
  C3 — layout parsing: minimal TS twin of `layout_parse.py` for the DSL subset (#/./D/W/space; name/wall_h directives) with the same validation (door/window must sit in a wall run); stairs cells tolerated but skipped (assembly v1 parity)
  C4 — golden test: assembled l-room (all 4 views) pixel-diffs ≤1% vs the Python PNGs (allow AA/codec tolerance), automated in the e2e or unit harness
tasks: T1 assets — copy full kit(json+6png)+4 goldens — test/unit/assets/* — low | T2 types — src/assemble/types.ts — medium | T3 layout-parse — src/assemble/layout-parse.ts — medium | T4 massing — src/assemble/massing.ts — medium | T5 assemble core — src/assemble/assemble.ts — high | T6 facade+CONTEXT — src/assemble/index.ts,CONTEXT.md — low | T7 parse/massing unit tests — test/unit/assemble-parse.test.ts — medium | T8 golden test+compositor — test/unit/assemble-golden.test.ts,test/unit/helpers/composite.ts — high
context: /mnt/workspace/code/isoroll-module/CONTEXT.md, /mnt/workspace/code/isoroll-module/src/CONTEXT.md, /mnt/workspace/core/skills/foundry.md, /mnt/workspace/core/skills/iso-visual.md, /mnt/workspace/code/isoroll-content/SCENE-CREATION.md (§ contract), Python reference: /mnt/workspace/code/isoroll-content/src/pipeline/{scene_assemble.py,layout_parse.py,layout_massing.py,scene_guide_render.py}
criticality-note: this becomes the painter's live re-render engine (P7) — keep it PURE (no Foundry document writes; rendering only), so painter can call it per stroke.

## Plan
branch: feature/ts-assembler (base: feature/module-walls-import)

Architecture stance (load-bearing, confirmed against repo — Loop 3 finalizes):
- vitest runs `environment: node` with a PIXI STUB (test/unit/setup.ts) — real `PIXI.RenderTexture` is NOT available in unit tests. Therefore `src/assemble/` stays PURE and returns a placement plan `AssembledScene { width, height, placements:[{name,x,y}] }` (dims + integer sprite positions). This IS the P7-reusable, per-stroke artifact.
- Compositing a placement plan into an actual image is an environment-specific SINK, kept OUT of `src/` (pngjs is a devDependency; importing it in src/ would break the Foundry runtime build). The C4 golden test composites via a node/pngjs helper under `test/`; the PIXI/offscreen-canvas sink is a ~15-line adapter deferred to P7 wiring (untestable in node anyway). C1's "composed scene image" is produced and diffed in the golden harness.
- Texture sizes come from the DECODED PNG dimensions (Python uses `sprites[name].size`), passed into `assembleScene` as a `textureSizes` map — NOT from kit.json `size`.
- Integer conversions use `Math.trunc` (Python `int()` truncates toward zero), never floor/round.

| id | task | files | done-when | tier | effort |
|----|------|-------|-----------|------|--------|
| T1 | Provision test assets: copy the FULL kit (kit.json + all 6 PNGs: floor, wall, door_u, door_v, window_u, window_v) from `/mnt/workspace/code/isoroll-content/output/kit-guide/` into `test/unit/assets/kit/`, and copy the 4 Python goldens `l-room_{SW,SE,NE,NW}.png` from `/mnt/workspace/code/isoroll-content/output/assembled/` into `test/unit/assets/assembled/`. Do NOT rely on existing `test/e2e/assets/kit/` (only 4 PNGs, no kit.json, wrong axes). | test/unit/assets/kit/*, test/unit/assets/assembled/* | 7 kit files (incl kit.json) + 4 golden PNGs present; kit.json parses with px_per_unit 96 and 6 pieces | low | low |
| T2 | Define TS types: View union SW/SE/NE/NW; KitPiece{origin:[number,number], size:[number,number]}; KitMeta{px_per_unit:number, wall_h:number, pieces:Record<string,KitPiece>}; Layout{name:string, grid:string[], wallH:number, rows:number, cols:number, errors:string[], warnings:string[]}; Opening{kind:"door"\|"window", offset:number}; Box{u0,v0,l,d,h:number, kind:string, openings:Opening[], axis:"u"\|"v"}; Placement{name:string, x:number, y:number}; AssembledScene{width:number, height:number, placements:Placement[]}. | src/assemble/types.ts | tsc clean; all types exported | medium | low |
| T3 | Port `layout_parse.py` subset. Constants WALL=# FLOOR=. VOID=space DOOR=D WINDOW=W; STAIRS={^,>,v,<}; SOLID={#,D,W}; KNOWN=SOLID∪STAIRS∪{".","space"}; DEFAULT_WALL_H=3. parseLayout(text,name): split leading `key: value` directives (line where split(":")[0].trim() is a valid identifier and grid not yet started) from grid lines; drop trailing blank grid lines; right-pad every grid row to max width; build Layout; validate. validate: per cell, error if char∉KNOWN; error if char∈{D,W} and NOT in a straight wall run. inWallRun(u,v): (kind(u-1,v)∈SOLID && kind(u+1,v)∈SOLID) OR (kind(u,v-1)∈SOLID && kind(u,v+1)∈SOLID). kind(u,v): grid char or VOID if out of bounds. rotateCw(layout,turns): for turns%4 iters set grid = transpose of reversed rows (Python `zip(*grid[::-1])`), remapping stair chars ^→>→v→<→^; recompute rows/cols; carry errors/warnings; grid stays rectangular. Read layout_parse.py in Carry context and match EXACTLY. | src/assemble/layout-parse.ts | T7 tests green; parseLayout(l-room) yields errors:[], cols=8, rows=8 | medium | medium |
| T4 | Port `layout_massing.py` merge=false lane. runOpenings(l,u,v,len,axis): for i in [0,len), cell along axis; DOOR→Opening("door",i), WINDOW→Opening("window",i). cellAxis(l,u,v): "u" if left OR right neighbor∈SOLID; else "v" if up OR down∈SOLID; else "u" (horizontal wins). floorBoxes(l): per row greedily merge consecutive FLOOR cells into ONE Box(u0,v0,run,1,0,"floor") — DO NOT emit per-cell floors (Python merges floor runs even in render lane). cellWallBoxes(l,wallH): one Box(u,v,1,1,wallH,"wall", runOpenings(l,u,v,1,"u"), cellAxis(l,u,v)) per SOLID cell. massing(l) returns floorBoxes(l) THEN cellWallBoxes(l,l.wallH), order preserved. Stairs SKIPPED (no step boxes — assembly never places them). | src/assemble/massing.ts | T7 tests green; l-room massing = 1 floor box per floor run + one wall box per #/D/W cell | medium | medium |
| T5 | Port `scene_assemble.py` core as a PURE function. VIEW_TURNS={SW:0,SE:1,NE:2,NW:3}; MARGIN=16; proj(u,v,z)=[(u-v), (u+v)*0.5 - z]. pieceFor(box): "floor" if kind=="floor"; if kind=="wall": openings.length ? `${openings[0].kind}_${axis}` : "wall"; else null. assembleScene(layout, kitMeta, textureSizes:Record<string,{w,h}>, view): turned=rotateCw(layout,VIEW_TURNS[view]); boxes=massing(turned) sorted STABLY by comparator (a,b)=>(a.h>0?1:0)-(b.h>0?1:0) OR (a.u0+a.v0)-(b.u0+b.v0) — ties keep massing order (stable sort / index tiebreak); s=kitMeta.px_per_unit. Per box: name=pieceFor(box); skip if name==null OR name∉kitMeta.pieces OR name∉textureSizes. px=(u0-v0)*s, py=(u0+v0)*0.5*s (proj at z=0 scaled by s); [ox,oy]=kitMeta.pieces[name].origin; left=px-ox, top=py-oy; w=textureSizes[name].w, h=textureSizes[name].h; xs push left & left+w; ys push top & top+h; record {name,left,top}. After loop: width=Math.trunc(maxX-minX)+2*MARGIN; height=Math.trunc(maxY-minY)+2*MARGIN; dx=MARGIN-minX, dy=MARGIN-minY; placements=records.map(r=>({name, x:Math.trunc(left+dx), y:Math.trunc(top+dy)})). Return {width,height,placements}. NO PIXI, NO pngjs imports in src/. | src/assemble/assemble.ts | T8 golden green | high | medium |
| T6 | Facade + doc. index.ts re-exports assembleScene, parseLayout, rotateCw, and all public types. Write CONTEXT.md (line 2 must be `> ...`): pure deterministic per-cell scene assembler, `scene_assemble.py` twin; returns placement plan; compositing is caller/test's job; P7 painter reuses per stroke. | src/assemble/index.ts, src/assemble/CONTEXT.md | npm run lint clean; facade imports resolve | low | low |
| T7 | Unit tests for T3+T4 (C3). Assert: parseLayout(l-room text) → errors empty, cols=8 rows=8; a synthetic layout with a D NOT in a wall run → errors non-empty; rotateCw(l,4).grid deep-equals original grid; a layout containing a stair char parses with no error AND massing emits no step/stair box; cellAxis horizontal-wins on a cell with both a horizontal and vertical solid neighbor. Run before impl → confirm RED for missing-behavior reasons. | test/unit/assemble-parse.test.ts | tests present; red pre-impl, green post-impl | medium | medium |
| T8 | Golden test (C4) + node compositor. test/unit/helpers/composite.ts (TEST-ONLY, may import pngjs): given AssembledScene + Record<name, decoded PNG rgba+dims>, build width×height RGBA buffer filled black (0,0,0,255), alpha-composite each placement with straight "over" (out = src + dst*(1-srcA), integer-rounded) at (x,y), return RGB PNG buffer. assemble-golden.test.ts: read test/unit/assets/kit/kit.json + decode 6 PNGs via pngjs → kitMeta + textureSizes(from decoded w/h) + rgba map; for each view SW,SE,NE,NW: assembleScene(...) → composite → pixelmatch vs test/unit/assets/assembled/l-room_{view}.png (per-pixel threshold 0.12); assert diffPixels/(w*h) ≤ 0.01 AND dims equal golden. | test/unit/assemble-golden.test.ts, test/unit/helpers/composite.ts | all 4 views ≤1% diff; npm run verify:fast green | high | high |

## Plan Review (adversarial, assume small executors)
- Existing `test/e2e/assets/kit/` is INCOMPLETE (4 PNGs, no kit.json) and has the WRONG axis pieces (has door_v/window_u; SW view needs door_u/window_v). Reusing it → missing-texture skips → golden fails. → FIX: T1 copies the full 6-PNG+json kit from `output/kit-guide/` into a fresh `test/unit/assets/kit/` and the row forbids reusing the e2e kit. Source verified to contain all 6 PNGs + kit.json.
- PIXI RenderTexture (C1 literal wording) is impossible in vitest node env (setup.ts stubs PIXI). → FIX: architecture stance splits the PURE placement core (src/) from the compositing sink (test/pngjs now, PIXI/P7 later); C1's "composed image" is produced+diffed in the golden harness. Recorded so Loop 3 does not design a PIXI dependency into src/.
- pngjs is a devDependency; importing it in `src/assemble/` would break the Foundry runtime build. → FIX: T5/T6 forbid pngjs in src/; the compositor lives in `test/unit/helpers/composite.ts` (T8).
- Truncation parity: Python `int()` truncates toward zero; `Math.floor` diverges for negative left/top/minX (u<v cells give negative proj x). → FIX: T5 mandates `Math.trunc` for width/height and placement x/y.
- Floor massing merges RUNS, not per-cell — naive per-cell floors would blow the diff. → FIX: T4 explicitly merges floor runs, one Box per run, cites Python parity.
- Texture size source: Python uses actual PNG dimensions, not kit.json `size`. → FIX: T5 takes `textureSizes` as a param; T8 populates from decoded PNG w/h.
- Sort ties: Python `sorted` is stable; TS `Array.sort` is stable in modern Node but the comparator must return 0 on ties. → FIX: T5 gives the exact comparator and requires ties preserve massing order (floors-before-walls), index tiebreak allowed.
- Piece selection must also gate on texture existence (Python `name not in sprites`). → FIX: T5 skips when name∉textureSizes.
- rotateCw correctness: `zip(*grid[::-1])` = reverse rows then transpose. → FIX: T3 spells out "transpose of reversed rows" + stair remap + rectangularity + rows/cols recompute; T7 asserts 4×rotate == identity.
- verify:fast scope = lint (src only) + vitest; new test files are not linted (`eslint src`), and the two pre-existing golden failures (b32-real-junction/golden-junction) are E2E (verify:full), so they do not affect this loop's gate. Confirmed against package.json + eslint config.
- Compositing rounding vs PIL alpha_composite may leave sub-1% AA-edge differences — within the C4 tolerance (≤1% + threshold 0.12; wrong piece/order/rotation produce whole-sprite diffs far exceeding 1%). Acceptable, not FATAL.

verdict: PASS

executor: loop-high model=opus tier=high
