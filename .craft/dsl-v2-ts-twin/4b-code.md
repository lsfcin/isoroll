# Loop 4b — Code Until Green — dsl-v2-ts-twin

## Carry
slug: dsl-v2-ts-twin | branch: loop/dsl-v2-ts-twin (base feature/ts-assembler) | root: code/isoroll-module
test-cmd: `npm run verify:fast` | e2e-cmd: `npm run verify:full` (needs Foundry at localhost:30000 — Loop 5 only if server up, else skip note)
criticality: normal | verdict: standard
criteria:
  C1 TS parser twin — parses SAME DSL v2 fixtures as Python (level blocks, attr grids side/type/wmat/fh, roof:/stair: group lines authoritative, R/S derived+validated, voxel-exclusivity + stair-incl∈{2.5,5} validation)
  C2 golden twin — TS parse output (levels, groups, massing GRP-box list) matches Python goldens structure-exact on all fixtures (round-trip toDsl==fixture + hand-derived group spans + error-class parity)
  C3 manifest consumption — module import path consumes v2 manifest fields: tile elevation from level/z0, WallDef.dir from opening side, boundHeight from z-run height; existing walls-import stays green
  C4 `npm run verify:fast` fully green (lint + unit suites)
tasks:
  T1 — copy 5 v2 fixtures → test/unit/assets/dsl-v2/*.txt — low
  T2 — extend types (Level, Group, Box.z0, kind "GRP", MARKERS/ROOF_FORMS/STAIR_TYPES/ENCLOSE) → src/assemble/types.ts — medium
  T3 — port layout-groups.ts (grpBaseData/grpCellVoxels/diagSolid, rig.frag geometry) → src/assemble/layout-groups.ts — high
  T4 — port v2 parser+validate (parseText dispatches to v2 on `level N:`; ports layout_parse.py v2 + layout_dsl_v2.py) → src/assemble/layout-parse.ts (+layout-dsl-v2.ts if over line-gate) — high
  T5 — port massing v2 (Box.z0 default 0 + one GRP box per group cell) → src/assemble/massing.ts — medium
  T6 — port serialize (toDsl round-trip) → src/assemble/layout-serialize.ts — medium
  T7 — manifest consumption C3 (ManifestTile.z, ManifestWall.dir, boundHeight z-run) → src/import/{manifest-types,import-tiles,import-walls}.ts — medium
  T8 — facade + CONTEXT + ROADMAP line → src/assemble/index.ts, src/assemble/CONTEXT.md, ROADMAP.md — low
context: code/CONTEXT.md, code/isoroll-module/CONTEXT.md, code/isoroll-module/SPECS.md, code/isoroll-module/src/CONTEXT.md
BRANCH WARNING: base loop/dsl-v2-ts-twin on feature/ts-assembler (verified: carries assembler 5efcbdb + walls-import f1c7d64). Do NOT branch off current dirty feature/floor-fog-spike working tree (uncommitted M to src/assemble/*, module.ts, ROADMAP.md; untracked load.ts/load.d.ts NOT on the base — do not depend on them). Loop 2 must set those aside before branching.
keep-trail: yes

## Code

Ported the Python twins (layout_groups.py, layout_dsl_v2.py, layout_parse.py's dispatch,
layout_massing.py, layout_serialize.py, scene_manifest.py's z/dir/boundHeight) line-for-line into
TS for T2–T7. Values hand-verified against the Python originals before writing (esp. grpBaseData/
grpCellVoxels on both groups in dsl_v2_groups.txt).

attempt 1 (logic): all T2–T7 files implemented → 0 test failures, all 13 new + 79 baseline green
immediately. But 33 lint errors from this project's custom gates (`local/single-return`,
`local/one-call-per-statement`, `local/max-chain-depth`, `max-lines-per-function`) that ported
branchy Python control flow trips directly, plus layout-dsl-v2.ts hit 263 lines on first Write
(over the 200-line size gate) → split into layout-dsl-v2.ts (grammar/parse) + NEW
layout-dsl-v2-validate.ts (validation), per 3-arch.md's own "if over line-gate" contingency.

attempt 2 (lint-only, no logic change): fixed all 33 errors — multi-return functions restructured
to one `let result; if/else; return result;`; chained calls (`.map().sort()`, multi-arg Math.*,
2-call template statements) split into intermediate variables; `level.g[0].length` (chain depth 3)
split via a `firstRow` variable; validateGroups split into collectGroupVoxels/reportDoubleBooked/
reportUncoveredMarkers to clear the 40-line function cap. Re-verified group math by hand after
restructuring — control flow only, no behavior change.

No RETURN needed: logic was correct on attempt 1; only iteration was closing local ESLint gates.

green: yes run: `npm run verify:fast` → lint 0 errors, 46 warnings (== pre-Loop-4a baseline);
vitest Test Files 17 passed (17) | Tests 92 passed (92) = 79 baseline + 13 new.

touched: src/assemble/{types,layout-groups,layout-dsl-v2,massing,layout-serialize}.ts (T2/T3/T5/T6),
src/assemble/layout-dsl-v2-validate.ts (NEW, T4 line-gate split), src/assemble/layout-parse.ts
(T4: one `if (HAS_LEVEL.test(text))` branch added to parseText; pre-existing floor-fog-spike
load()-removal at file top left untouched — the one plan file that unavoidably overlaps the
no-touch list), src/import/{manifest-types,import-tiles,import-walls}.ts + src/walls/wall-types.ts
(T7, PIN-4).

Deliberately NOT touched (T8, deferred): src/assemble/index.ts, ROADMAP.md — no new test imports
through the index.ts barrel, so T8 isn't required for C4; both are on the contamination list.
src/assemble/CONTEXT.md's routing table WAS auto-regenerated by this project's post-edit doc-gen
hook (picks up new/changed files automatically) — not a manual edit, and was already dirty before
this loop (Loop 4a's stubs triggered the same regen). Confirmed untouched: src/core/module.ts,
src/spike-floor/, test/unit/spike-*, test/e2e/spike-floor.spec.mjs, test/e2e/assets/assembled/,
ROADMAP.md's P6 spike line. Flagging for Loop 6 diff-scope review, same as 4a.

executor: loop-medium model=sonnet tier=medium
