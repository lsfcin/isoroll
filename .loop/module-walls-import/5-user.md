## Carry
slug: module-walls-import | branch: feature/module-walls-import | root: /mnt/workspace/code/isoroll-module
test-cmd: `npm run verify:fast` | e2e-cmd: `npm run verify:full` (Foundry server ALREADY RUNNING at localhost:30000 — do NOT restart it; log at scratchpad foundry.log)
criticality: normal | verdict: standard
criteria:
  C1 — programmatic import API `isoroll.importSceneManifest(manifest, {sceneId?})`: given a scene manifest JSON (shape: {scene, view, pxPerVoxel, tiles[{piece,asset,facing,u,v,boundHeight,imageOffset,pxPerVoxel}], walls[WallDef-shaped]}), creates Foundry tiles (with `flags.isoroll.boundHeight`/`imageOffset` set from manifest, texture = kit asset) and walls via the existing `createWallsFromDefs` (`src/walls/wall-crud.ts`)
  C2 — counts round-trip: created wall documents == manifest walls length (l-room fixture: 6); created tiles == manifest tiles length (34); door entries in manifest become door walls
  C3 — malformed manifest (missing keys, anchor outside [0,1]², unknown facing) → graceful error (notification/throw with message), zero partial documents created
  C4 — e2e: import the real l-room manifest into a live Foundry scene; `verify:full` green (no regressions in existing B-spec/golden suites)
tasks:
  T1 — manifest types + pure structural validator (mirror wall_schema.py + wall-types.d.ts) — src/import/manifest-types.ts, src/import/manifest-validate.ts — medium
  T2 — pure tile-data mapping (u,v→x,y at grid size; flags.isoroll.boundHeight+imageOffset; texture.src=staged kit asset) — src/import/import-tiles.ts — high
  T3 — manifest wall[] → WallDef[] for createWallsFromDefs incl. scene-normalized→tile-relative anchor contract + door config — src/import/import-walls.ts — high
  T4 — orchestrator importSceneManifest(validate-first, atomic) + register on isoroll global — src/import/import-scene-manifest.ts, src/core/module.ts — high
  T5 — stage kit PNGs under Foundry Data so texture.src resolves (simplest path; Loop 3 confirms) — kit assets → foundrydata modules path — medium
  T6 — unit tests (T1/T2/T3) + e2e spec import real l-room + register in run.mjs — test/unit/*, test/e2e/import-manifest.spec.mjs, test/e2e/run.mjs — medium
context: /mnt/workspace/code/isoroll-module/CONTEXT.md, /mnt/workspace/code/isoroll-module/src/CONTEXT.md, /mnt/workspace/code/isoroll-module/src/walls/CONTEXT.md, /mnt/workspace/core/skills/foundry.md (+ subfiles as needed), /mnt/workspace/core/skills/iso-visual.md, /mnt/workspace/code/isoroll-content/SCENE-CREATION.md (§ contract)

## User Test
scenario: As the GM, I drop a fresh scene, run `isoroll.importSceneManifest` against the real l-room manifest (the artifact `isoroll-content`'s pipeline actually produces), and expect a fully furnished, structurally-sound room in one call: every floor/wall tile placed, every wall segment present with its height/anchor flags intact, no stray door where the source room has none, and — the part that actually matters to a player — the walls I just imported physically block movement through them, not just exist as inert documents.
script: `.loop/module-walls-import/user-test-scenario.mjs` (new, this loop; not added to the committed suite/`test/e2e/run.mjs` — an ad hoc Loop 5 artifact, reuses `test/e2e/helpers.mjs`) run: `node .loop/module-walls-import/user-test-scenario.mjs` (Foundry already running at localhost:30000, per Carry)
observed:
- `Loop5 user-test PASS: {"tileIds": 34, "wallIds": 6, "sceneTiles": 34, "sceneWalls": 6, "doorCount": 0, "tile0Flags": {"presetEnabled": true, "boundHeight": 0, "baseElevation": 0, "imageOffset": {"x": 0.5, "y": 0.07142857142857142}}, "collision": {"crossesWall": true, "openField": false}}`
- chained real path confirmed: fresh scene `fx-loop5-walls-import` → `globalThis.isoroll.importSceneManifest(l-room manifest)` → 34 tiles + 6 walls created and persisted on `canvas.scene` → door count round-trips (0 doors in this fixture, 0 door walls created — no spurious doors) → tile0's `flags.isoroll.boundHeight`/`imageOffset` match the manifest source values exactly (C1's flag-population claim, not previously asserted by any existing test) → vision/wall-blocking observation: since `test/e2e/helpers.mjs`'s own "Oracles" section is explicitly JSON-only ("no vision", helpers.mjs:126) and exposes no ready-made collision oracle, the scenario calls Foundry's public `CONFIG.Canvas.polygonBackends.move.testCollision` directly (read-only, no new production code) — a movement ray through an imported wall's midpoint collides (`crossesWall: true`), a control ray 1000px outside every wall's bounding box does not (`openField: false`): the imported walls are functionally blocking, not just document rows.
- also re-ran the full e2e-cmd from Carry for the record: `npm run verify:full` → lint 0 errors (46 pre-existing warnings, unchanged) → unit 40/40 pass → e2e `8 specs, 2 failed, 0 xpass`: `PASS import-manifest (3.4s)` plus 5 other B-specs green; `FAIL b32-real-junction (8.36% px diff)` and `FAIL golden-junction (10.45% px diff)` — identical failure signatures (byte-for-byte same px-diff percentages) to 4b's isolated-rerun evidence that these two are pre-existing on develop, independent of `module-walls-import`. Not this feature's failure; carried forward as the same open item 4b flagged.
matches-expected-result: yes — C1 (import API creates tiles+walls with flags from manifest, confirmed via tile0Flags match), C2 (34 tiles/6 walls/0-doors round-trip, confirmed), C4 (real l-room import through the live public API, no regression in the 6 non-golden e2e specs) all observed directly on the chained real path, not re-derived from unit tests. C4's literal "`verify:full` green" sub-clause does not hold end-to-end only because of the 2 pre-existing golden-image failures (b32-real-junction, golden-junction) — confirmed non-regressions, orthogonal to this feature, already flagged by 4b and reconfirmed here with matching failure magnitudes on a fresh run.

Flag for the record (not a return — same pre-existing item 4b already flagged, reconfirmed unchanged on this run): `b32-real-junction` and `golden-junction` golden-image specs still fail on HEAD independent of `module-walls-import` (8.36% / 10.45% px diff, matching 4b's isolated-rerun numbers) — worth a separate ticket to recapture goldens or investigate environment drift; does not block this feature's ship.

executor: loop-medium model=sonnet tier=medium
