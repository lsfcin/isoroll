node_modules/
dist/
*.log
.env
.env.*

.tsbuildinfo-declarations

# Facade interface files are redundant (index.ts is already minimal)
src/**/index.d.ts

# codegraph local index
.codegraph/
test/e2e/output/

# Console-paste helpers are not a compilation unit — the stub generator must not claim this dir.
test/manual/jsconfig.json
